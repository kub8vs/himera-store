<section class="simple-page">
  <p class="breadcrumb reveal in-view"><a href="{{ routes.root_url }}">STRONA GŁÓWNA</a> / {{ page.title | upcase }}</p>
  <h1>{{ page.title }}</h1>
  <div class="rich-text">{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Strona treści",
  "settings": []
}
{% endschema %}
